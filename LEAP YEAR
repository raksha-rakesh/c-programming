#include<stdio.h>
void main(){
    int a;
    printf("Enter a year");
    scanf("%d",&a);
    if(a/4==0)
    {else if(a/400==0 || a/100==0)
        printf("%d The year is leap year",a);
    }
    else
        printf("%d Not a year leap year",a);
}
