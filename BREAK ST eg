#include<stdio.h>
void main()
{
    int a;
    int sum=0;
    while(1)
    {
        printf("enter the num");
        scanf("%d",&a);
        if(a==0)
            break;
        sum=sum+a;
    }
    printf("%d is the total",sum);
}
