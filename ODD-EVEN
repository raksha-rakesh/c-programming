#include<stdio.h>
void main()
{
    int a;
    printf("Enter a number:");
    scanf("%d",&a);
   {
       if(a%2==0)
        printf("the number is even",a);
       else
        printf("the number is odd",a);

    }
}
